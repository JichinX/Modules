if (!isPlugin.toBoolean()) {
    apply from: 'https://gitee.com/Sir_XU/codes/qmpdo91yxb8lan04z2frk91/raw?blob_name=package'
}
android {
    compileSdkVersion compile_version
    defaultConfig {
        minSdkVersion min_version
        targetSdkVersion target_version
        versionCode 112
        versionName "1.1.2"
        multiDexEnabled true
    }

    buildTypes {

        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            if (!isPlugin.toBoolean()) {
                signingConfig signingConfigs.release
            }
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}
dependencies {
    compileOnly fileTree(include: ['*.jar'], dir: 'libs')
    implementation "com.android.support:appcompat-v7:$support"
    implementation 'com.android.support:multidex:1.0.3'
    implementation "id.zelory:compressor:$compressor"
    implementation 'com.king.zxing:zxing-lite:1.0.0'
    //所需模块
//    implementation project(':web')
    implementation 'me.xujichang.modules:web:1.0.0'
//    implementation project(':basic')
    implementation 'me.xujichang.modules:basic:1.0.2'

//    implementation project(':ui')
    implementation 'me.xujichang.modules:ui:1.0.2'
}
apply from: '../base_upload.gradle'
